name: incus-core-e2e

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions:
  contents: read

concurrency:
  group: incus-core-e2e-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

env:
  GOTOOLCHAIN: local

jobs:
  incus-standalone:
    runs-on: ubuntu-26.04
    timeout-minutes: 30
    env:
      HACO_CI_RUNNER_ENVIRONMENT: github-hosted
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Validate standalone Incus E2E helpers
        run: bash -n tools/ci-incus.sh test/e2e/incus_standalone.sh
      - name: Install and initialize Incus substrate
        run: bash tools/ci-incus.sh setup
      - name: Verify standalone Incus system containers
        run: bash tools/ci-incus.sh standalone
      - name: Capture standalone Incus diagnostics
        if: failure()
        run: bash tools/ci-incus.sh diagnostics
      - name: Upload standalone Incus diagnostics
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: incus-standalone-diagnostics
          path: ${{ runner.temp }}/incus-diagnostics/
          if-no-files-found: error
          retention-days: 7
      - name: Clean up standalone Incus resources
        if: always()
        run: bash tools/ci-incus.sh cleanup-standalone

  incus-core-e2e:
    needs: incus-standalone
    runs-on: ubuntu-26.04
    timeout-minutes: 30
    env:
      HACO_CI_RUNNER_ENVIRONMENT: github-hosted
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version: 1.27.x
          check-latest: true
          cache: false
      - name: Validate Core E2E helper syntax
        run: bash -n tools/ci-incus.sh tools/ci-incus-core.sh tools/ci-incus-project-cleanup.sh tools/ci-storage-helper.sh test/e2e/incus.sh
      - name: Prepare proven Incus substrate for Hacocoon Core
        run: bash tools/ci-incus.sh setup
      - name: Allow leased workspace owner in Incus root idmap
        run: |
          uid="$(id -u)"
          gid="$(id -g)"
          if ! awk -F: -v id="$uid" '$1 == "root" && id >= $2 && id - $2 < $3 { found=1 } END { exit found ? 0 : 1 }' /etc/subuid; then
            printf 'root:%s:1\n' "$uid" | sudo tee -a /etc/subuid >/dev/null
          fi
          if ! awk -F: -v id="$gid" '$1 == "root" && id >= $2 && id - $2 < $3 { found=1 } END { exit found ? 0 : 1 }' /etc/subgid; then
            printf 'root:%s:1\n' "$gid" | sudo tee -a /etc/subgid >/dev/null
          fi
      - name: Install real Btrfs privilege helper
        run: bash tools/ci-storage-helper.sh setup
      - name: Run Hacocoon egress policy against real Incus
        run: bash tools/ci-incus-core.sh egress
      - name: Run Hacocoon Core and shipped CLI lifecycle against real Incus
        env:
          TMPDIR: ${{ runner.temp }}/haco-storage-helper-e2e
        run: bash tools/ci-incus.sh core
      - name: Capture Hacocoon and Incus diagnostics
        if: failure()
        run: |
          bash tools/ci-incus.sh diagnostics
          bash tools/ci-incus-core.sh diagnostics
          bash tools/ci-storage-helper.sh diagnostics
      - name: Upload Core diagnostics
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: incus-core-diagnostics
          path: ${{ runner.temp }}/incus-diagnostics/
          if-no-files-found: warn
          retention-days: 7
      - name: Delete CI-owned Incus projects and pools
        if: always()
        run: bash tools/ci-incus-project-cleanup.sh
      - name: Clean up Hacocoon Core E2E resources
        if: always()
        run: bash tools/ci-incus.sh cleanup-core
      - name: Clean up storage helper resources
        if: always()
        run: bash tools/ci-storage-helper.sh cleanup

  incus-vscode-e2e:
    needs: incus-standalone
    runs-on: ubuntu-26.04
    timeout-minutes: 30
    env:
      HACO_CI_RUNNER_ENVIRONMENT: github-hosted
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version: 1.27.x
          check-latest: true
          cache: false
      - name: Validate VS Code user-journey E2E helpers
        run: bash -n tools/ci-incus.sh tools/ci-incus-project-cleanup.sh tools/ci-storage-helper.sh tools/ci-vscode-e2e.sh test/e2e/vscode.sh
      - name: Prepare fresh real Incus substrate
        run: bash tools/ci-incus.sh setup
      - name: Allow linked-worktree owner in Incus root idmap
        run: |
          uid="$(id -u)"
          gid="$(id -g)"
          if ! awk -F: -v id="$uid" '$1 == "root" && id >= $2 && id - $2 < $3 { found=1 } END { exit found ? 0 : 1 }' /etc/subuid; then
            printf 'root:%s:1\n' "$uid" | sudo tee -a /etc/subuid >/dev/null
          fi
          if ! awk -F: -v id="$gid" '$1 == "root" && id >= $2 && id - $2 < $3 { found=1 } END { exit found ? 0 : 1 }' /etc/subgid; then
            printf 'root:%s:1\n' "$gid" | sudo tee -a /etc/subgid >/dev/null
          fi
      - name: Install real Btrfs privilege helper
        run: bash tools/ci-storage-helper.sh setup
      - name: Run linked-worktree VS Code journey through real loopback SSH
        env:
          TMPDIR: ${{ runner.temp }}/haco-vscode-e2e
        run: bash tools/ci-vscode-e2e.sh
      - name: Capture VS Code user-journey diagnostics
        if: failure()
        run: |
          bash tools/ci-incus.sh diagnostics
          bash tools/ci-storage-helper.sh diagnostics
      - name: Upload VS Code user-journey diagnostics
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: incus-vscode-user-journey-diagnostics
          path: ${{ runner.temp }}/incus-diagnostics/
          if-no-files-found: warn
          retention-days: 7
      - name: Delete CI-owned Incus projects and pools
        if: always()
        run: bash tools/ci-incus-project-cleanup.sh
      - name: Clean up Hacocoon sandbox resources
        if: always()
        run: bash tools/ci-incus.sh cleanup-core
      - name: Clean up storage helper resources
        if: always()
        run: bash tools/ci-storage-helper.sh cleanup

  windows-wsl-acceptance:
    runs-on: windows-2025
    timeout-minutes: 35
    continue-on-error: true
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Validate optional Windows/WSL acceptance helper
        shell: pwsh
        run: |
          $errors = $null
          [void][System.Management.Automation.Language.Parser]::ParseFile(
            (Resolve-Path 'test/e2e/windows_wsl.ps1'),
            [ref]$null,
            [ref]$errors
          )
          if ($errors.Count -ne 0) {
            $errors | Format-List | Out-String | Write-Error
            exit 1
          }
      - name: Attempt real Windows -> WSL2 -> Ubuntu 26.04 -> systemd -> Incus acceptance
        shell: pwsh
        run: ./test/e2e/windows_wsl.ps1
